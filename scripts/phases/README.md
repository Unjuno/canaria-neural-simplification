# Active phase-specific runners

Only phase-specific code still referenced by the current reviewed evidence remains here.

- `training_time/` — G7–G27 training-time/consolidation runners and controls.

The older versioned runner sequence (`v11`, `v17`–`v23`) moved to `../../archives/research-history/scripts/` during the pre-announcement repository restructure tracked by Issue #16.

Archived runners are preserved for provenance and may contain historical paths or environment assumptions. They are not presented as a stable public API.
