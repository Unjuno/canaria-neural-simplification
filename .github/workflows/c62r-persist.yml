name: c62r-persist-evidence

on:
  push:
    branches:
      - research/c62r-p2-frontier-mechanism
    paths:
      - '.github/workflows/c62r-persist.yml'

permissions:
  contents: write
  actions: read

jobs:
  persist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download locked C62R aggregate artifact
        uses: actions/download-artifact@v4
        with:
          name: c62r-aggregate-decision
          path: /tmp/c62r-aggregate
          github-token: ${{ github.token }}
          repository: Unjuno/canaria-neural-simplification
          run-id: 33660473069
      - name: Persist rows and decision without alteration
        run: |
          mkdir -p results/gaussian_shift_interface/c62r
          cp /tmp/c62r-aggregate/rows.json results/gaussian_shift_interface/c62r/FRESH_ROWS.json
          cp /tmp/c62r-aggregate/decision.json results/gaussian_shift_interface/c62r/DECISION.json
          python - <<'PY'
          import hashlib
          from pathlib import Path
          for p in [
              Path('results/gaussian_shift_interface/c62r/FRESH_ROWS.json'),
              Path('results/gaussian_shift_interface/c62r/DECISION.json'),
          ]:
              print(p, hashlib.sha256(p.read_bytes()).hexdigest())
          PY
      - name: Commit persisted evidence
        run: |
          git config user.name 'canaria-evidence-bot'
          git config user.email 'canaria-evidence-bot@users.noreply.github.com'
          git add results/gaussian_shift_interface/c62r/FRESH_ROWS.json results/gaussian_shift_interface/c62r/DECISION.json
          git commit -m 'Persist C62R fresh rows and locked exploratory decision'
          git push origin HEAD:research/c62r-p2-frontier-mechanism
