name: c70e-persist-evidence

on:
  push:
    branches:
      - research/c70e-p64-reference-repair
    paths:
      - '.github/workflows/c70e-persist.yml'

permissions:
  contents: write
  actions: read

jobs:
  persist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download locked C70E aggregate artifact
        uses: actions/download-artifact@v4
        with:
          name: c70e-aggregate-decision
          path: /tmp/c70e-aggregate
          github-token: ${{ github.token }}
          repository: Unjuno/canaria-neural-simplification
          run-id: 33997084158
      - name: Persist rows and decision without alteration
        run: |
          mkdir -p results/gaussian_shift_interface/c70e
          cp /tmp/c70e-aggregate/rows.json results/gaussian_shift_interface/c70e/FRESH_ROWS.json
          cp /tmp/c70e-aggregate/decision.json results/gaussian_shift_interface/c70e/DECISION.json
          python - <<'PY'
          import hashlib
          from pathlib import Path
          for p in [
              Path('results/gaussian_shift_interface/c70e/FRESH_ROWS.json'),
              Path('results/gaussian_shift_interface/c70e/DECISION.json'),
          ]:
              print(p, hashlib.sha256(p.read_bytes()).hexdigest())
          PY
      - name: Commit persisted evidence
        run: |
          git config user.name 'canaria-evidence-bot'
          git config user.email 'canaria-evidence-bot@users.noreply.github.com'
          git add results/gaussian_shift_interface/c70e/FRESH_ROWS.json results/gaussian_shift_interface/c70e/DECISION.json
          git commit -m 'Persist C70E fresh rows and locked P64-reference decision'
          git push origin HEAD:research/c70e-p64-reference-repair
